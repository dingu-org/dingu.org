export const siteUrl = "https://dingu.org";
export const orgId = `${siteUrl}/#organization`;
export const websiteId = `${siteUrl}/#website`;
export const webpageId = `${siteUrl}/#webpage`;
export const projectsId = `${siteUrl}/#projects`;
